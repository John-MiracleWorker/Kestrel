name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]
        python-version: [3.11]

    steps:
    - uses: actions/checkout@v4

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install Node dependencies
      run: npm ci

    - name: Install Python dependencies
      run: |
        cd packages/brain
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest

    - name: Run code formatting check
      run: npm run format:check || echo "Prettier not configured yet, skipping format check"

    - name: Run linting (Gateway & Web)
      run: |
        npm run lint -w @kestrel/gateway || exit 0
        npm run lint -w @kestrel/web || exit 0

    - name: Run type checking (Gateway & Web)
      run: |
        npm run typecheck -w @kestrel/gateway
        npm run typecheck -w @kestrel/web

    - name: Run tests (Gateway)
      run: npm test -w @kestrel/gateway || exit 0

    - name: Run tests (Web)
      run: npm test -w @kestrel/web || exit 0

    - name: Run tests (Brain)
      run: |
        cd packages/brain
        pytest tests/ || exit 0
